import React from 'react'
import styles from '../../style'

const Events = () => {
  return (
    <main id="events" className={styles.boxWidth}>
        Eventos
    </main>
  )
}

export default Events